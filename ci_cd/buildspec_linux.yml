# This is a WIP to deploy infrastructure automatically
# https://docs.aws.amazon.com/codebuild/latest/userguide/build-spec-ref.html
version: 0.2

phases:
  install:
    commands:
      - tests/utils.sh install_conda
  build:
    commands:
      - tests/utils.sh run_tests_conda_
